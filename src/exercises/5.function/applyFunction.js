import { sayHello } from "./sayHello";

/**
 * Write a function called `greetEveryone` that takes a parameter `names` which is an array of name (String).
 * This function will call `sayHello` to greet each person in the array.
 * Notes: if name is not provided (undefined or empty), return "Hello, World!"
 */
function greetEveryone(names) {
  // Start your exercise here
  // End your exercise here
}

export { greetEveryone, sayHello };
