/**
 * ----------------------------------
 * Create a list of your pet, each pet has the following properties:
 * - name: type String
 * - age: type Number
 * - isMale: type Boolean
 * Then assign this list to a variable called myPets
 * ----------------------------------
 */
export function defineArrayType() {
  let myPets = [];
  // Start your exercise here

  // End your exercise here
  return myPets;
}
