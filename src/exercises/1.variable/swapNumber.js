/**
 * ----------------------------------
 * Fix the code below so that it runs correctly (add new line of codes, if needed)
 * ----------------------------------
 */
export function swapNumber(a, b) {
  // Start your exercise here
  const a = b;
  let b = a;
  // End your exercise here
  return { a, b };
}
