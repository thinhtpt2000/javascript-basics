/**
 * ----------------------------------
 * 1. Define two variables a, b with any values you want (b cannot be 0).
 * 2. Declare variables: sum, difference, product, quotient
 * to store the results of the arithmetic operations: +, -, *, /
 * ----------------------------------
 */
export function defineVariables() {
  // Start your exercise here
  // End your exercise here
  return { a, b, sum, difference, product, quotient };
}
